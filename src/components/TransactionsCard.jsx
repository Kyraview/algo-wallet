export default function TransactionsCard(props){
    if(props.transactions.length === 0){
        
    }

    return(
        <div style={{overflowY:'scroll', overflowX:'hidden', width:'100%'}} className="transactions-card">

        </div>
    )
}